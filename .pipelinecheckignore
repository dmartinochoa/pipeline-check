# Pipeline-check suppressions for this repo's own workflows.
#
# Format: CHECK_ID:RESOURCE per line. Comments start with ``#``.
# Auto-discovery picks this file up at cwd; no flag needed.
#
# Most prior entries here became redundant once the catalog gained
# structural detection: ``actions/deploy-pages`` (docs.yml) marks
# Pages-only workflows that the artifact heuristic now skips, and a
# job-level ``AWS_ENDPOINT_URL`` pointing at localhost (LocalStack)
# now exempts the affected jobs from GHA-005 and GHA-014. Only the
# residual cases that the catalog can't see structurally remain.

# ── localstack-test.yml — HTML report uploads, not software builds ──
# The terraform-fixture job uploads HTML pipeline-check reports via
# ``actions/upload-artifact@``. The signing / SBOM / vuln-scan /
# SLSA-attest checks treat any upload-artifact use as a software
# release; there's no further structural signal to distinguish "test
# report" uploads from real builds, so we suppress per-resource here.
GHA-006:.github/workflows/localstack-test.yml
GHA-007:.github/workflows/localstack-test.yml
GHA-020:.github/workflows/localstack-test.yml
GHA-024:.github/workflows/localstack-test.yml
# Inline ``pip install pyyaml`` and ``pip install -e .`` in the test
# setup install the package being tested. The hash-locked install
# (``pip install --require-hashes -r requirements-dev.txt``) runs
# immediately before, covering the actual dependency surface.
GHA-021:.github/workflows/localstack-test.yml

# ── python-app.yml — lint / type-check / test runner ────────────────
# The hash-locked ``--require-hashes -r requirements-dev.txt`` install
# covers the actual dependency surface; ruff / mypy ride that
# lockfile too. The remaining ``pip install -e .`` of this package
# is what the suite tests against.
GHA-021:.github/workflows/python-app.yml

# ── release.yml — SLSA generator must be tag-pinned, not SHA ────────
# The SLSA generator is consumed as a reusable workflow that produces
# Sigstore-signed provenance. ``slsa-verifier`` validates the
# attestation by matching the trusted-builder ref against an allow-
# list of known SLSA generator semantic-version tags. A SHA pin
# produces an unrecognized ref and breaks every downstream
# verification call (the consumer-side ``slsa-verifier verify-
# artifact --source-uri ...`` documented in the README). This is the
# one place SHA-pinning is the wrong call.
GHA-025:.github/workflows/release.yml
