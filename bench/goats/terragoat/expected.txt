# Check IDs the terragoat is intended to demonstrate. Direct-HCL
# parsing shipped post-1.3.0 and goats.yml scans via --tf-source.
# Populate by running:
#   python bench/goat_runner.py --goat terragoat --suggest
# then hand-curate against terragoat's documented misconfigurations.
