-r requirements.txt
hypothesis>=6.156.6
jsonschema>=4.0
# Pin away from mypy 2.0: it tightens `no-untyped-call` against
# PyYAML helpers we rely on (``construct_object``, ``construct_document``,
# ``dispose``) and a try/except CSafeLoader / SafeLoader fallback in
# ``checks/base.py``. Lift this pin together with the annotations
# fix as a separate change — out of scope for the lockfile-hygiene
# PR that first added mypy to ``requirements-dev.txt``.
mypy>=2.1.0,<3.0
pytest>=7.4.0
pytest-benchmark>=5.0.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
ruff>=0.15.13
types-PyYAML>=6.0
typing-extensions>=4.4.0
