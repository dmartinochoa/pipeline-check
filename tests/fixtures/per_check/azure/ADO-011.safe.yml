pr:
  branches:
    include: [main]
resources:
  repositories:
    - repository: templates
      type: git
      name: shared/ci-templates
      ref: refs/tags/v1.4.2
stages:
  - template: ci/build.yml@templates
