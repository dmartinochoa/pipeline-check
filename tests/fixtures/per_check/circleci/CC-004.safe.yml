version: 2.1
jobs:
  build:
    docker: [{ image: cimg/base:stable }]
    environment:
      LOG_LEVEL: info
    steps:
      - checkout
workflows:
  main:
    jobs:
      - build:
          context: db-prod
