version: 2.1
jobs:
  build:
    docker: [{ image: cimg/base:stable }]
    steps: [checkout]
  deploy-prod:
    docker: [{ image: cimg/base:stable }]
    steps:
      - checkout
      - run: ./scripts/deploy.sh
workflows:
  main:
    jobs:
      - build
      - hold-for-approval:
          type: approval
          requires: [build]
      - deploy-prod:
          requires: [hold-for-approval]
