version: 2.1
jobs:
  test:
    docker: [{ image: cimg/base:stable }]
    steps:
      - checkout
      - run: npm test
      - store_test_results:
          path: test-results
workflows:
  main:
    jobs: [test]
