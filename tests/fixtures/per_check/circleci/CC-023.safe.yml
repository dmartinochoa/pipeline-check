version: 2.1
jobs:
  build:
    docker: [{ image: cimg/base:stable }]
    steps:
      - checkout
      - run: curl -o pkg.tgz https://internal-mirror.local/pkg.tgz
workflows:
  main:
    jobs: [build]
