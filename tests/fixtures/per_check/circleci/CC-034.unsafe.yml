version: 2.1
jobs:
  train:
    docker:
      - image: cimg/python:3.12
    steps:
      - run:
          name: Load model
          command: python -c "from transformers import AutoModel; AutoModel.from_pretrained('acme/llm', trust_remote_code=True)"
workflows:
  build:
    jobs:
      - train
