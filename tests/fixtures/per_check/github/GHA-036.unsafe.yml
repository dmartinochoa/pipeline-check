name: build
on:
  workflow_call:
    inputs:
      runner:
        type: string
        required: true
jobs:
  build:
    runs-on: ${{ inputs.runner }}
    steps:
      - uses: actions/checkout@v4
      - run: make build
