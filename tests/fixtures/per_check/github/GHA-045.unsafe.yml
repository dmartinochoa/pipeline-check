name: build-release
on:
  workflow_dispatch:
    inputs:
      ref:
        description: Tag or branch
        required: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - run: make release
