name: spread
on: push
jobs:
  push-elsewhere:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - env:
          TARGET_URL: https://github.com/other-org/other-repo.git
        run: git push $TARGET_URL main
