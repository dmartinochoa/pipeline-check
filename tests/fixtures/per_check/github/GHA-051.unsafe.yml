name: integration
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        ports: ['5432:5432']
    steps:
      - uses: actions/checkout@v4
      - run: make integration
