name: typosquat-uses-safe
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Canonical ``actions/checkout``, SHA-pinned for GHA-001 too.
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - run: ./build.sh
