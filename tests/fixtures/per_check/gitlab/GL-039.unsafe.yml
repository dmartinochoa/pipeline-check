build-image:
  services:
    - name: docker:27-dind
      command: ["--host=tcp://0.0.0.0:2375"]
  variables:
    DOCKER_TLS_CERTDIR: ""
    DOCKER_HOST: "tcp://docker:2375"
  script:
    - docker build -t app .
