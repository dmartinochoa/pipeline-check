pipeline {
  agent { label 'build' }
  stages {
    stage('Release') {
      steps {
        sh 'syft . -o cyclonedx-json > sbom.json'
        sh 'cosign sign --yes app:$BUILD_TAG'
      }
    }
  }
}
