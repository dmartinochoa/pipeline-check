pipeline {
  agent any
  stages {
    stage('Setup') {
      steps {
        sh 'curl -fsSL https://example.com/install.sh | bash'
      }
    }
    stage('Build') {
      steps {
        sh 'make build'
      }
    }
  }
}
