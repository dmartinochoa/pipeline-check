pipeline {
  agent { label 'build' }
  stages {
    stage('Build') {
      steps { sh 'make build' }
    }
    stage('Run packaging') {
      steps {
        build job: 'downstream-package'
      }
    }
  }
}
