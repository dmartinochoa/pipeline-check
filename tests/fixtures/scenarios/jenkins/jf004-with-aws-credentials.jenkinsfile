pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                withAWS(credentials: 'aws-prod-key', region: 'us-east-1') {
                    sh 'aws s3 sync build/ s3://my-bucket/'
                }
            }
        }
    }
}
